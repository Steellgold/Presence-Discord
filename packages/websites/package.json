{
  "name": "@dp/websites",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "pnpm generate && tsc -b",
    "clean": "rimraf dist",
    "generate": "node scripts/generate-presence-registry.mjs",
    "typecheck": "pnpm generate && tsc -b --pretty false"
  }
}
